# LangGraph 学习项目依赖（Python 3.11，当前 .venv 实测版本）
# 新电脑：  pip install -r requirements.txt

langgraph==1.2.5
langgraph-prebuilt==1.1.0
langgraph-checkpoint==4.1.1

# 第 06 域「SqliteSaver」课用到，当前 .venv 尚未安装，新机请一并装上
langgraph-checkpoint-sqlite

# 第 10 域「create_supervisor」课用到，当前 .venv 尚未安装，新机请一并装上
langgraph-supervisor

# 第 01 域「图可视化」print_ascii() 用到（draw_mermaid 不需要它）
grandalf

langchain==1.3.9
langchain-core==1.4.7
langchain-openai==1.3.2
openai==2.41.1

python-dotenv==1.2.2
pydantic==2.13.4
